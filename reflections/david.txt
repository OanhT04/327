I implemented and fixed some bugs regarding the evaluations and experiments. I did this by allowing us to collect throughput (req/s) and latency (median/95th) for AVAIL and RESERVE when testing Sync RPC baseline. For Async + pub/sub stress testing, I enabled sensors to send 10 updates/sec/lot while running the same client loads; measured impact on RPC tail latency and pub/sub event delivery behavior (including back-pressure policy). 